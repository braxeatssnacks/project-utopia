const phaserMethods = { preload: preload, create: create, update: update };
var game = new Phaser.Game($('#game').width(), $('#game').height(), Phaser.AUTO, 'game', phaserMethods);

function preload() {}

function create() {}

function update() {}